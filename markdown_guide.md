# Markdownによる本ドキュメントの編集方法

## はじめに

このドキュメントはMarkdown形式で作成されています。Markdownは軽量マークアップ言語で、読みやすく書きやすい形式で文書を作成でき、HTMLなどの他の形式に変換することができます。

## 環境構築

Markdownファイルの編集には特別な環境は必要ありません。任意のテキストエディタで編集可能です。

### 推奨エディタ

- **Visual Studio Code**: Markdown プレビュー機能が標準搭載されています
- **Typora**: リアルタイムプレビューが可能なMarkdownエディタ
- **その他**: Atom、Sublime Text、Notepad++など、お好みのエディタをご使用ください

### Visual Studio Code の場合

1. VS Codeをインストールします
2. Markdownファイルを開きます
3. `Ctrl+Shift+V` (Mac: `Cmd+Shift+V`) でプレビューを表示できます
4. エディタとプレビューを並べて表示するには `Ctrl+K V` (Mac: `Cmd+K V`) を使用します

### Gitの設定

ドキュメントの変更を管理するため、Gitの使用を推奨します。

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@address"
```

## Markdownの基本文法

### 見出し

見出しは `#` を使って表します。`#` の数が見出しのレベルを表します。

```markdown
# 見出し1
## 見出し2
### 見出し3
#### 見出し4
```

### 強調

- **太字**: `**テキスト**` または `__テキスト__`
- *イタリック*: `*テキスト*` または `_テキスト_`
- ***太字イタリック***: `***テキスト***`

### リスト

#### 箇条書きリスト

```markdown
- 項目1
- 項目2
  - サブ項目2-1
  - サブ項目2-2
- 項目3
```

#### 番号付きリスト

```markdown
1. 第一項目
2. 第二項目
3. 第三項目
```

### リンク

```markdown
[リンクテキスト](URL)
[GitHub](https://github.com)
```

### 画像

```markdown
![代替テキスト](画像のパス)
![ロゴ](img/kuwanolab-logo.png)
```

### コード

#### インラインコード

バッククォート `` ` `` で囲みます。

```markdown
`コード`
```

#### コードブロック

3つのバッククォート ` ``` ` で囲みます。言語を指定することでシンタックスハイライトが適用されます。

````markdown
```python
def hello():
    print("Hello, World!")
```

```cpp
#include <iostream>
int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
```
````

### 表

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| データ1 | データ2 | データ3 |
| データ4 | データ5 | データ6 |
```

表示結果：

| 列1 | 列2 | 列3 |
|-----|-----|-----|
| データ1 | データ2 | データ3 |
| データ4 | データ5 | データ6 |

### 引用

`>` を使って引用を表します。

```markdown
> これは引用文です。
> 複数行にわたって
> 引用することもできます。
```

### 水平線

`---`、`***`、`___` のいずれかを使用します。

```markdown
---
```

### チェックリスト

```markdown
- [ ] 未完了のタスク
- [x] 完了したタスク
```

表示結果：
- [ ] 未完了のタスク
- [x] 完了したタスク

## ドキュメント作成のベストプラクティス

### 構造を意識する

文書の見た目（スタイル）ではなく構造（意味や役割など）を意識してください。適切な見出しレベルを使用し、論理的な構造を保ちましょう。

### 画像の配置

画像は `img/` ディレクトリに配置し、相対パスで参照します。

```markdown
![説明テキスト](img/サブディレクトリ/画像名.png)
```

### ファイル名とパス

- ファイル名は半角英数字を使用することを推奨します
- スペースの代わりにアンダースコア `_` またはハイフン `-` を使用します
- 日本語ファイル名も使用可能ですが、互換性のため英数字を推奨します

### 数式の表記

Markdownでは、数式を表現するためにLaTeX形式の記法が使用できます（プレビューツールによって対応が異なります）。

```markdown
インライン数式: $E = mc^2$

ブロック数式:
$$
\varepsilon_a = \left(1 - \frac{h}{h_{ref}}\right) \times 100
$$
```

### リンクの管理

- 外部リンクは完全なURLを記述します
- 内部リンク（同じドキュメント内）はアンカーを使用します

```markdown
[別の章へのリンク](#見出しのテキスト)
```

## GitHub Pages での公開

このドキュメントはGitHub Pagesで自動的に公開されます。

1. `main`または`master`ブランチにプッシュします
2. GitHub Actionsが自動的にビルドを実行します
3. ビルドされたサイトがGitHub Pagesにデプロイされます

公開されたドキュメントは以下のURLでアクセスできます：
https://mkt-kuno.github.io/DigitShowDoc/

## トラブルシューティング

### プレビューで画像が表示されない

- 画像のパスが正しいか確認してください
- 相対パスを使用している場合、現在のファイルからの相対位置が正しいか確認してください

### リンクが機能しない

- URLが正しいか確認してください
- 内部リンクの場合、見出しのテキストが正確に一致しているか確認してください（スペースはハイフンに変換されます）

### 文字化けが発生する

- ファイルのエンコーディングがUTF-8であることを確認してください

## 参考資料

- [Markdown公式ガイド](https://www.markdownguide.org/)
- [GitHub Flavored Markdown仕様](https://github.github.com/gfm/)
- [Markdown記法チートシート](https://www.markdownguide.org/cheat-sheet/)
