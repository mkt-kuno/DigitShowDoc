# ドキュメント作成ガイド

本ドキュメントは GitHub Flavored Markdown (GFM) で書かれています。
GitHub リポジトリ上で直接閲覧でき、PDF 生成は行いません。

## 構成

各ドキュメントは 1 ファイルで構成されます。

| ファイル | 内容 |
|---------|------|
| `quickstart.md` | クイックスタート |
| `user_manual.md` | ユーザーマニュアル |
| `developer_manual.md` | デベロッパーマニュアル |

## 見出し構造

各ファイルの先頭に `# ドキュメント名` を置き、`\chapter` に相当させます。
`\section` → `##`、`\subsection` → `###`、`\subsubsection` → `####` とします。

先頭の `#` 直後に `## 目次` を配置し、このファイルの `##` セクションへのリンク一覧を記載します。

```markdown
# クイックスタート

## 目次

- [事前準備：ショートカットの作成](#事前準備ショートカットの作成)
- [実験前：アプリを開く](#実験前アプリを開く)
```

### GitHub スラッグの規則

GitHub のアンカーリンク（スラッグ）は以下のように生成されます。

- 小文字化
- 日本語・英数字・ハイフン・アンダースコア以外の文字を除去
- スペースはハイフンに変換

例: `## 供試体を設置する前：ロードセルのキャリブレーション値の入力`
→ `#供試体を設置する前ロードセルのキャリブレーション値の入力`

同一ファイル内で 2 回以上現れる見出しには `-1`、`-2` のサフィックスが付きます。
例: `## CH4-7:空きチャンネル` が 2 回出現 → 2 回目は `#ch4-7空きチャンネル-1`

## 本文スタイル

### テキスト装飾

| LaTeX | Markdown |
|-------|----------|
| `\texttt{...}` | `` `...` `` |
| `\emph{...}` | `**...**` |
| `\url{...}` | URL そのまま（自動リンク化） |
| ``` ``...'' ``` | `"..."` （カーリークォート） |
| `\%` `\&` `\_` | `%` `&` `_` |

### リスト

- itemize → `-`
- enumerate → `1.` `2.` `3.`…
- description の `\item[用語] 説明` → `- **用語**: 説明`

ネスト時は親リストの本文開始位置に揃えてインデントします。

### 数式

GitHub は MathJax により数式をレンダリングします。

- インライン: `$...$`
- ディスプレイ: `$$` で囲み、`align` 環境はそのまま使用:

```markdown
$$
\begin{align}
    h_\mathrm{BC} &= h_\mathrm{init} - \delta_\mathrm{BC} \\
\end{align}
$$
```

`siunitx` の `\SI{100}{\kPa}` 等は `$100\,\mathrm{kPa}$` のように直接数式で記述します。
`physics2` の `\ab(x)` は `\left(x\right)` に置き換えます。

### コードブロック

`lstlisting` 環境はフェンスコードブロックに変換します。
言語指定は `C++` → `cpp`、JSON → `json`、指定なし → 言語タグなしとします。

````markdown
**CDigitShowModbus.cpp より抜粋**

```cpp
#define DSM_AO_DEF_VLT_MOTOR_ON (5.0f)
```
````

コード内容（インデント・タブ・コメント）は一字一句そのまま保持してください。

### 図

`includegraphics` は Markdown の画像記法に変換します。

```markdown
![メイン画面。](img/DSM_main.png)

*図: メイン画面。*
```

キャプションは日本語のまま、`*図: キャプション*` 行として配置します。

### 表

`tabular` 環境は GFM パイプテーブルに変換します。
`\caption` は `*表: キャプション*` として表の直前に置きます。
列揃えは `tabular` の指定に従い、`:---`（左揃え）、`---:`（右揃え）、`:-:`（中央揃え）とします。

```markdown
*表: HX711 の物理値と換算式*

| 物理値 | 差圧（128倍）$[\mathrm{mV}]$ | 電圧$[\mathrm{mV}]$ |
|------:|------:|------:|
| 32767 | 2048 | 8 |
```

## 相互参照

GitHub Markdown には図表の自動番号機能がないため、手動でリンクを張ります。

- 同一ファイル内: `[表示テキスト](#スラッグ)`
- 他ファイル: `[表示テキスト](developer_manual.md#スラッグ)`
- 図・表はその章の `##` セクションへリンクします

例:

```markdown
[コントロールについて](#コントロールについて)を参照してください。

（本文中の図への参照）
[Pre-Consolidation](#pre-consolidation)節の図を参照。
```

`\cref{sec:loadcell_cal}` は `[供試体を設置する前：ロードセルのキャリブレーション値の入力](#供試体を設置する前ロードセルのキャリブレーション値の入力)節` のように変換してください。

## ファイル構成

```text
DigitShowDoc/
├── quickstart.md          # クイックスタート
├── user_manual.md         # ユーザーマニュアル
├── developer_manual.md    # デベロッパーマニュアル
├── howtotex.md            # ドキュメント作成ガイド（このファイル）
├── README.md              # プロジェクト概要
├── img/                   # 画像ファイル
│   ├── DSM_*.png
│   ├── dev/
│   │   └── *.png
│   ├── vs2022/
│   │   └── *.png
│   └── precon_q_rpm.png
```

## 注意事項

- pandoc や LuaLaTeX は使用しません。全ての Markdown ファイルは手書きで作成します。
- 画像は事前に `img/` に配置し、パスは相対パスで指定します。
- 数式内の LaTeX コマンド（`\mathrm`、`\left`、`\right`、`\frac` 等）はそのまま残します。
